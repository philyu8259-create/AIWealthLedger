# 国际版真机联调执行记录

## 2026-04-13 / Round 1 baseline
- device:
  - `Phil的iPhone` (`00008110-000451840A80401E`)
  - `iPhone 17 Pro Max` Simulator (`F7171F49-3E5C-4257-85F6-9630B844F681`)
- build:
  - 待执行真机启动：`flutter run -d 00008110-000451840A80401E --debug --dart-define=APP_FLAVOR=intl`
- env:
  - 项目根目录 `.env` 与 `ios/Runner/.env` 当前已同步
  - `GOOGLE_IOS_CLIENT_ID` / `GOOGLE_SERVER_CLIENT_ID` / `GOOGLE_IOS_REVERSED_CLIENT_ID` = 已配置
  - `GEMINI_API_KEY` / `GOOGLE_VISION_API_KEY` / `FINNHUB_API_KEY` = 为空
- passed:
  - `flutter analyze` 通过
  - `flutter devices` 可识别真机与模拟器
  - 当前构建已具备国际登录基础配置
  - 当前构建可直接验证“缺 key 诚实提示”路径
  - 模拟器首页截图确认主 UI 为英文，无明显中文残留
  - 模拟器 `--route=/settings` 截图确认设置页为英文，`Provider Status` 区块已出现
  - 模拟器 `--route=/analysis` 回归通过，缺少 `GEMINI_API_KEY` 时会显示清晰英文提示
- blocked:
  - 无法完整验证 intl AI / OCR / 美股的真实 provider 成功链路，因为对应 key 尚未配置
  - 资产页截图自动分析本轮超时，需后续人工或分块截图复核美股缺 key 前台文案
- notes:
  - 本轮优先验证：启动稳定性、英文 UI、国际登录、缺 key 提示链路
  - 已执行真机启动：`flutter run -d 00008110-000451840A80401E --debug --dart-define=APP_FLAVOR=intl`
  - 结果：构建、安装、启动成功，`ConfigService` 已从设备包内 `.env` 读取到 16 个变量，Splash 正常进入已登录态，账单 / VIP / 云端同步链路正常跑通
  - 使用 `--route=/analysis` 做模拟器直达回归时，抓到 `PredictionPage` 在 `initState()` 过早访问 `AppStrings.of(context)` 的生命周期异常；已改为 `didChangeDependencies()` 首次加载，`flutter analyze` 通过，回归后异常消失
  - 使用 `--route=/analysis` 截图确认英文缺 key 提示文案清晰：Gemini 未配置时会直接提示不可用
  - 观察到一条原生日志：`Target native_assets required define SdkRoot but it was not provided`，当前未阻断 app 启动，后续可单独复核是否只是 debug 环境噪音
  - 额外执行 `openclaw doctor --non-interactive`：未见安全红灯；主要是 howard 会话存储存在缺失 transcript 与 orphan transcript，属于环境维护项，不阻断当前 app 联调
  - 用户再次反馈“手机上打开 app 又闪退”后，已重新切回真机排查。`flutter run --release` 因 Flutter 误找 `build/ios/iphoneos/Runner.app` 失败，但实际 Release 产物存在于 `build/ios/Release-iphoneos/Runner.app`。随后已用 `xcrun devicectl device install app` 手工安装 Release 包到真机，并用 `xcrun devicectl device process launch --terminate-existing com.phil.AIAccountant` 成功前台拉起。
  - 2026-04-13 14:xx 已完成三方 API 直连体检：Finnhub 搜索/行情 200 正常；Gemini key 可识别但返回 429，原因是当前账号/项目 `generate_content_free_tier_*` 配额为 0；Google Vision key 可识别但返回 403，原因是项目 `396571310206` 未开启 billing。结论：美股链路已具备真联调条件；AI / OCR 还差 Google 侧 billing / quota 打开。
